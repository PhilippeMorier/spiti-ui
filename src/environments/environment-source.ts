// The content of this file will be overwritten by the current environment during build.
// The build system defaults to the `dev` environment, which doesn't exist.
// Use e.g. `ng build --env=production` to use the content of `environment.production.ts`.
// The list of which environment maps to which file can be found in `.angular-cli.json`.

import { Environment } from './environment';

export const environment: Environment = {
  firebase: {},
  name: 'DEVELOPMENT',
  production: false,
};
